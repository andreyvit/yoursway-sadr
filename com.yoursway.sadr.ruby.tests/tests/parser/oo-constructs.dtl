obj1 = circle:new( 20 )
obj1 = rectangle:new( 10,15 )

type(obj:surface())
type(obj2:surface())

procedure point:show()
	type(self:classname())
	super:show()
end

procedure point:init( x, y ); self:moveto( x,y ); end

dtl$object:subclass("imp$object", "iPID,iFID,iUserData")

dtl$object::$clear()

type(c.x)

type(image.origin.x)
image.origin.x = 30

